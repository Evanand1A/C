#include<stdio.h>
int main()
{
    // 1 Add two numbers
    int a,b;
    printf("Ënter two numbers to be added:");
    scanf("%d %d",&a,&b);
    printf("The sum of the given two numbers is:%d %d\n",a+b);
}
